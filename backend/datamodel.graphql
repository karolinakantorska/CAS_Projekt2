type User {
  id: ID! @id
  email: String! @unique
  password: String!
  resetToken: String
  resetTokenExpiry: Float
  name: String
  surname: String
  description: String
  photo: String
  permissions: [Permission] @scalarList(strategy: RELATION)
}
enum Permission {
  ADMIN
  GUIDE
  USER
}
