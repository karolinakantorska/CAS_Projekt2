type User {
  id: ID! @id
  password: String!
  email: String! @unique
  name: String!
  surname: String
  resetToken: String
  resetTokenExpiry: Float
  description: String
  photo: String
  permissions: Permission!
  reservations: [Reservation]! # to jest teraz zle
}
enum Permission {
  ADMIN
  GUIDE
  USER
}
type Day {
  id: ID! @id
  day: String!
  reservation: [Reservation]
}
type Reservation {
  id: ID! @id
  time: Time
  guide: User! #relationship to the User
  day: Day! #relationship to the Day
  dayID: ID
  guideID: ID
  userName: String
  userEmail: String
  nrOfPeople: String
  description: String
}
enum Time {
  AM
  PM
  DAY
}
