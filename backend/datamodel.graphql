type User {
  id: ID! @id
  password: String!
  email: String! @unique
  name: String!
  surname: String
  resetToken: String
  resetTokenExpiry: Float
  description: String
  photo: String
  permissions: Permission!
}
enum Permission {
  ADMIN
  GUIDE
  USER
}
