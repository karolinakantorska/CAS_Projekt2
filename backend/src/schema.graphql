type Query {
  users: [User!]!
}
type Mutation {
    createUser(
      email: String!
      name: String
      surname: String
      description: String
    ): User!
}
type User {
  id: ID!
  email: String!
  name: String
  surname: String
  description: String
}
